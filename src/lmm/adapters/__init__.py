"""Doors onto other people's frameworks.

Each adapter is a TRANSLATION, never a second implementation: it hands the
caller's framework what that framework expects and calls the same `Memory`
verbs a Python caller would. Nothing here decides what may be said.
"""
